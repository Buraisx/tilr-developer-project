<header class="row header">
  <div class="col-6">
    <%= link_to('javascript:history.back()', {class: "header__back-button"}) do %>
      <i class="fa fa-chevron-left"></i>
    <% end %>
    <h2 class="header__title"><%= title -%></h2>
  </div>
  <%= yield %>
</header>